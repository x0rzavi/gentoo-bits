# These settings were set by the catalyst build script that automatically
# built this stage.
# Please consult /usr/share/portage/config/make.conf.example for a more
# detailed example.
COMMON_FLAGS="-march=native -fomit-frame-pointer -O2 -pipe"
CFLAGS="${COMMON_FLAGS}"
CXXFLAGS="${COMMON_FLAGS}"
FCFLAGS="${COMMON_FLAGS}"
FFLAGS="${COMMON_FLAGS}"

# NOTE: This stage was built with the bindist Use flag enabled
PORTDIR="/var/db/repos/gentoo"
DISTDIR="/var/cache/distfiles"
PKGDIR="/var/cache/binpkgs"

# This sets the language of build output to English.
# Please keep this setting intact when reporting bugs.
LC_MESSAGES=C

# X0rzAvi's configs
USE="pipewire screencast vaapi vulkan"
USE="${USE} lto pgo graphite clang"
USE="${USE} -a52 -cdda -cdr -cups -dvd -dvdr -ppds"

RUBY_TARGETS="ruby30"
PYTHON_TARGETS="python3_10"
PYTHON_SINGLE_TARGET="python3_10"
LLVM_TARGETS="X86 AMDGPU"

MAKEOPTS="-j8 -l8"
EMERGE_DEFAULT_OPTS="--jobs=8 --load-average=8 --ask --verbose"
PORTAGE_NICENESS=10
PORTAGE_IONICE_COMMAND="ionice -c 3 -p \${PID}"
ACCEPT_KEYWORDS="~amd64"
ACCEPT_LICENSE="*"
GRUB_PLATFORMS="efi-64"
FEATURES="candy parallel-install parallel-fetch"
VIDEO_CARDS="amdgpu"
INPUT_DEVICES="libinput"
L10N="en en-US"
CPU_FLAGS_X86="aes avx avx2 f16c fma3 mmx mmxext pclmul popcnt rdrand sha sse sse2 sse3 sse4_1 sse4_2 sse4a ssse3"
